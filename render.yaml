# File: render.yaml
services:
  - type: web
    name: lts-ws-probe
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn probe:app --host 0.0.0.0 --port $PORT
